spring:
  application:
    name: hrs

  datasource:
    driver-class-name: org.postgresql.Driver
    name: nexign
    url: jdbc:postgresql://localhost:5434/nexign
    username: postgres
    password: postgres
  liquibase:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

server:
  port: 7654

cdr:
  generate:
    path: cdr.txt
    plus-cdr-path: cdr-plus.txt
